export interface Character {
    name: string
    height: string
    birth_year: string
  }

  export interface Planet {
    name: string
    climate: string
    terrain: string
    population: string
  }
  
  export interface Starship {
    name: string
    model: string
    manufacturer: string
    cost_in_credits: string
    length: string
  }